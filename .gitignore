# OS junk
.DS_Store

# Build artifacts
*.tsbuildinfo
dist/
node_modules/

# Test / tool output
*.out

# Nand2Tetris: never commit the downloaded tool suite (goes outside repo per 00-setup)
nand2tetris/tools/
nand2tetris-2.*/ /nand2tetris/*.zip
